Started with a simple JavaScript Hello World program to update the DOM
Added React and ReactDOM using CDN links
Created an <h1> element using React.createElement()
Rendered the element to the DOM using ReactDOM.createRoot() and .render()
Learned that React elements are not HTML, they are JavaScript objects (with props and children)
Created nested elements using createElement() similar to HTML structure
Learned how to add multiple children inside a React element
Understood that the order of script tags matters when using React via CDN
Learned why React is a library and not a framework
Understood that React can be used in specific parts of an application, not the entire app